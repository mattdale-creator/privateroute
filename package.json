{
  "name": "privateroute",
  "private": true,
  "version": "0.1.0",
  "description": "PrivateRoute — commercial WireGuard VPN control plane (P0)",
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/node-agent"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "dev:agent": "npm run dev -w apps/node-agent",
    "build": "npm run build -w apps/api && npm run build -w apps/web && npm run build -w apps/node-agent",
    "start:api": "npm run start -w apps/api",
    "start:agent": "npm run start -w apps/node-agent"
  }
}
